dotenv
  # .env.local  (or .env.production if you have a prod build)
  NEXT_PUBLIC_SUPABASE_URL=https://ztcejxyfcadzxoobqkaa.supabase.co
  NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_KGDgARm90UnkPLh6ZQ7HLQ_U4KmIUgd


  # Service‑role key – used **only** by Cloudflare Workers (never exposed to the
  browser)
  SUPABASE_SERVICE_ROLE_KEY=service_role|<LONG‑BASE64‑KEY>

  # JWT secret used to sign the short‑lived cookie that ties a Supabase
  # access‑token to our own session.  Any random 32‑byte string will do.
  JWT_SECRET=super‑secret‑32‑chars‑here

  # Cloudflare Workers environment variables (populated in the dashboard)
  #   – they are *not* stored in .env, they live in the Workers UI.
  #   Use the same names below so your Worker code can read `env.SUPABASE_URL`
  #   etc.
  # ----------------------------------------------------------------
  # (No values here – set them in the Cloudflare dashboard → Settings → Environment
  Variables)
